{
  "name": "Mask-Gitcoin-Donor-Subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ dimensiondev/mask-gitcoin-donor",
    "create-local": "graph create --node http://localhost:8020/ dimensiondev/mask-gitcoin-donor",
    "remove-local": "graph remove --node http://localhost:8020/ dimensiondev/mask-gitcoin-donor",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dimensiondev/mask-gitcoin-donor"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
